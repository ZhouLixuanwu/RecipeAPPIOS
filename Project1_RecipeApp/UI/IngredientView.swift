//
//  IngredientView.swift
//  Project1_RecipeApp
//
//  Created by LoveIrelia on 7/27/23.
//

import UIKit

class IngredientView: UIView {

    //var ingredientsStackView: UIStackView!
    //        // Create the vertical stack view to hold the horizontal IngredientViews
    //        ingredientsStackView = UIStackView()
    //        ingredientsStackView.translatesAutoresizingMaskIntoConstraints = false
    //        ingredientsStackView.axis = .vertical
    //        ingredientsStackView.spacing = 6
    //        view.addSubview(ingredientsStackView)
    //        ingredientsStackView.translatesAutoresizingMaskIntoConstraints = false
    //
    //        // Add constraints for the vertical stack view
    //        NSLayoutConstraint.activate([
    //            ingredientsStackView.topAnchor.constraint(equalTo: ingredientsLabel.bottomAnchor, constant: 10),
    //            ingredientsStackView.leadingAnchor.constraint(equalTo: contentView.leadingAnchor, constant: 5),
    //            ingredientsStackView.trailingAnchor.constraint(equalTo: contentView.trailingAnchor, constant: -5)
    //        ])
    //
    //        // Populate the vertical stack view with the horizontal IngredientViews
    //        if let recipeIngredients = recipe.recipeIngredient, let ingredientsSet = recipeIngredients as? Set<RecipeIngredient> {
    //            for recipeIngredient in ingredientsSet {
    //                let ingredientView = IngredientView() // horizontal stackview
    //                ingredientView.nameLabel.text = recipeIngredient.ingredient?.name
    //                ingredientView.quantityLabel.text = "\(recipeIngredient.quantity)"
    //                ingredientView.unitsLabel.text = recipeIngredient.ingredient?.unit
    //                ingredientsStackView.addArrangedSubview(ingredientView)
    //            }
    //        }
}
